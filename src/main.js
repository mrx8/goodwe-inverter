// const Inverter = require('./inverter')

async function main () {
  // const inverter = await Inverter({address: '192.168.30.219'})
  // const inverter = await Inverter({address: '192.168.30.203'})
  // console.log('inv', inverter)
  // const response = await inverter.send(Buffer.from('WIFIKIT-214028-READ'))
  // console.log('res', response)
}

main()
